// IMPORTANT: Order of the enum values matters!
public enum LayerName
{
    // Built-in
    Default,
    TransparentFX,
    IgnoreRaycast,
    Ground,
    Water,
    UI,

    // Roles
    Seer,
    Guardian,
    Mafia,

    // General
    Ghost,
    Player,

    // Environment
    GoThroughGroundPlayer,
    Corpse,
    GhostPassable,
    NameTag,
    CharacterRenderTexture,
    LocalPlayer
}